function [ output_args ] = movement( pos, fs, f )
%UNTITLED3 Summary of this function goes here
%   Detailed explanation goes here
n = ceil(fs/f);
N = floor(size(pos,1)/n);
sumVecX = zeros(N,1);
sumVecY = zeros(N,1);
for i=1:N
    sumVecX(i,1) = var(pos(N*(i-1)+2:N*i,1)-pos(N*(i-1)+1:N*i-1,1))
    sumVecY(i,1) = var(

end

